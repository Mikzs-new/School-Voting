name: CI/CD - Voting System

on:
    push:
        branches: [main]
    pull_request:
        branches: [main]
        types: [opened, synchronize, reopened]
    
jobs:
    name: Build, Test and Push
    runs on: ubuntu-latest

    steps:
        
